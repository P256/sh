apiVersion: v1 
kind: Deployment
metadata: 
  name: nginx-deployment
  labels:
    app: demo
spec:
  replicas: 2
  template:
    metadata:
      labels:
        name: demo
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 8080